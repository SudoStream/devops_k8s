apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: amateur-screenwriter
spec:
  replicas: 2
  selector:
    matchLabels:
      app: amateur-screenwriter
  strategy:
    type: RollingUpdate
  minReadySeconds: 10
  template:
    metadata:
      labels:
        app: amateur-screenwriter
        tier: backend
        bump: bump-32
    spec:
      imagePullSecrets:
        - name: myregistrykey
      volumes:
        - name: "amateur-screenwriter-tls"
          secret:
            secretName: "amateur-screenwriter-tls"
        - name: "nginx-amateur-screenwriter-dev-proxf-conf"
          configMap:
            name: "nginx-amateur-screenwriter-dev-proxf-conf"
            items:
              - key: "nginx-amateur-screenwriter.conf"
                path: "nginx-amateur-screenwriter.conf"
      containers:
        - name: nginx
          image: nginx:1.7.9
          imagePullPolicy: Always
          resources:
            limits:
              cpu: 100m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          ports:
            - containerPort: 443
          lifecycle:
            preStop:
              exec:
                command: ["/usr/sbin/nginx", "-s", "quit"]
          volumeMounts:
            - name: "nginx-amateur-screenwriter-dev-proxf-conf"
              mountPath: "/etc/nginx/conf.d"
            - name: "amateur-screenwriter-tls"
              mountPath: "/etc/tls"
        - name: amateur-screenwriter
          image: eu.gcr.io/api-event-horizon-151020/amateur-screenwriter:0.0.2
          imagePullPolicy: Always
          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka.dev.sudostream.io:9092
          resources:
            limits:
              cpu: 100m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi
          ports:
            - containerPort: 9000
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 180
            timeoutSeconds: 1
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 180
            timeoutSeconds: 5
