apiVersion: v1
kind: Service
metadata:
  name: antagonist-cast-service
  labels:
    run: antagonist-cast-service
spec:
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
  selector:
    app: actress
  type: LoadBalancer
