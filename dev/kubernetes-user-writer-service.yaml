apiVersion: v1
kind: Service
metadata:
  name: user-writer
  labels:
    run: user-writer
spec:
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
  selector:
    app: user-writer
  type: LoadBalancer
